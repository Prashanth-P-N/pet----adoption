async function start() {
  const wetherPromise = await fetch("https://api.weather.gov/gridpoints/MFL/110,50/forecast");
  const wetherData = await wetherPromise.json();
  const ourTmperature, wetherData, properties, peripds, []; 0; Tmperature;
}
